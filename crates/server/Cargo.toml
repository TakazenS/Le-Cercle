[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8.9"
tokio = { version = "1.52.3", features = ["full"] }
shared = { path = "../shared" }
sqlx = { version = "0.9.0", features = ["runtime-tokio", "tls-rustls", "postgres"] }
dotenvy = "0.15.7"

